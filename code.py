Yes, proceed by creating a copy of the original notebook. Disable the `insert_in_chunks(...)` call and both stored-procedure `EXEC` calls. Do not change the original notebook or any production table. Keep all read and calculation logic unchanged, and export only `global_df` to an Excel file for this initial review.
