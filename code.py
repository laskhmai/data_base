Proceed with Option 1. Replace `applymap()` with `map()` only in the read-only test copy. Do not change the original notebook or any business/calculation logic. Document this as a local Pandas compatibility change, confirm again that no database-write or stored-procedure calls are active, then rerun and export `global_df` to Excel.
