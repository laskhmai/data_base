Can we verify which Google service account credential version the Terraform workspace is actually consuming from Vault? Since the key is active in Vault, I'm wondering if the workspace is still using an older version after rotation.