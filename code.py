Proceed only in the read-only test copy. Comment out the single-resource condition `AND id = '{id}'`. Do not remove or change any other business filters or calculation logic. Keep all database writes and stored-procedure calls disabled.

Before running, show me the exact change and confirm how many distinct resources the unfiltered query will process. Then run a small sample of 10 resources first and export the results to Excel. If that completes correctly, stop and show me the results before running all resources.
