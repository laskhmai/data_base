Run the read-only test in batches of 50 resources. After completing each batch, immediately save its results to a separate Excel file before starting the next batch. If the process stops, resume from the next unfinished batch. Keep all database writes and stored-procedure calls disabled. Do not change the original notebook or production data.