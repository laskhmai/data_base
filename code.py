o the site-extensions deployment is failing in QA because all the function apps are deploying at the same time and Azure can't handle that many concurrent ARM deployments — it just throws "aggregated error too large" instead of the real error.
